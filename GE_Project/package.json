{
  "name": "automatic-work-table",
  "version": "1.0.0",
  "private": true,
  "description": "Automatic Work Table with SQLite user authentication and scheduling",
  "author": "Your Name",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "test": "node --test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "docker:build": "docker build -t work-table-app:latest .",
    "docker:run": "docker compose up -d"
  },
  "keywords": [
    "authentication",
    "scheduling",
    "work-table",
    "sqlite",
    "express"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
